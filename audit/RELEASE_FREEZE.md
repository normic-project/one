# Superseded pre-audit release freeze

The prior `v1.0.0-preaudit` Solidity and release hashes were intentionally invalidated by the event-only architecture change. They do not identify the current deployment candidate.

No replacement audit freeze or tag is created by this implementation task. Current contract and release hashes are generated in `reports/release-hash.json` for deployment preparation only.
